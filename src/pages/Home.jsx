import React from 'react';
import { Header, HeroSection, QualityProducts } from '../components';

function Home() {
    return (
        <main className="relative">
            <Header />
            <HeroSection />
            <QualityProducts />
        </main>
    );
}

export default Home;
